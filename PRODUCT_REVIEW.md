# Memoir 제품 전략 및 리뷰

**일시:** 2026-02-12
**참석자:** PM (진행), UI/UX Master, UX Design Expert, 파운더
**제품 버전:** master 브랜치 (커밋 53e790d 기준)
**문서 상태:** v2.1 — 전략 회의(6라운드 Q&A) + 파운더 인터뷰 결과 반영

---

## Part 1. 제품 비전 및 전략

### 1.1 제품 정체성

Memoir는 **포트폴리오 수준의 완성도**를 목표로 하되, 우선 **파운더 자신이 매일 사용할 수 있는 도구**를 만드는 것이 첫 번째 목표다. 학습 목적은 병행하며, 향후 준비가 되면 외부 사용자도 받을 수 있는 수준으로 발전시킨다.

### 1.2 핵심 가치

**"통합 기억 관리"** — 수집 → 자동 정리 → 소크라테스 대화 → 저널 회고

사용자의 지식과 경험을 하나의 흐름으로 연결한다:
1. **수집**: 웹, 카카오톡, 크롬 익스텐션 등 다양한 채널에서 기억을 모은다
2. **자동 정리**: AI가 수집된 정보를 분류하고 요약하여 구조화한다
3. **소크라테스 대화**: AI와의 대화를 통해 기억을 탐색하고 새로운 연결을 발견한다
4. **저널 회고**: 하루를 돌아보며 기억을 내면화하고 글로 정리한다

### 1.3 타겟 사용자

**현재 단계:** 파운더 자신 + 주변 지인 (dogfooding). "내가 매일 쓰면서 발견하는 문제를 해결하는" 단계다.

**장기 타겟:** 지식 노동자 — 연구자, 개발자, 기획자 등 정보를 많이 소비하고 정리할 필요가 있는 사람들. 이들의 공통 문제: "읽고 저장했지만 다시 찾지 못하는" 정보가 쌓인다. Memoir는 이 문제를 해결한다. 단, 외부 사용자 모집은 dogfooding으로 제품이 충분히 검증된 이후에 진행한다.

### 1.4 차별화 3축

| 축 | 설명 | 기존 도구와의 차이 |
|---|------|-------------------|
| **멀티채널 수집** | 웹/카카오톡/크롬익스텐션에서 원클릭 수집 | Notion, Obsidian은 수동 입력 위주 |
| **AI 자동 정리** | Librarian 에이전트가 분류/요약/태깅 | 기존 도구는 사용자가 직접 정리 |
| **소크라테스 대화** | 저장된 기억을 기반으로 한 맥락형 AI 대화 | ChatGPT는 사용자의 기억을 모름 |

### 1.5 디바이스 전략

**데스크톱 + 태블릿 우선.** 모바일은 후속 대응.

- Journal의 3-panel 구조, GraphView의 3D 시각화 등 핵심 경험이 넓은 화면에서 최적화됨
- 태블릿은 반응형으로 대응 (P1 사이드바 + JournalView 레이아웃)
- 모바일 앱은 수집 중심의 경량 경험으로 별도 설계 (P2 이후)

---

## Part 2. 핵심 기능 전략 결정 (Q&A 결과)

### 2.1 수집 채널: 3채널 동등 투자

| 채널 | 현재 상태 | 전략 |
|------|----------|------|
| **웹 (직접 입력)** | 동작 중 | 유지 + UX 개선 |
| **카카오톡** | 웹훅 동작 중 | 유지 + 에러 핸들링 강화 |
| **크롬 익스텐션** | v1.1 (OAuth + Fly.io 연동) | 유지 + 기능 강화 (P2-9) |

3개 채널 모두 핵심 수집 경로이므로 편향 없이 동등하게 투자한다. 특정 채널을 우선하거나 축소하지 않는다.

### 2.2 Chat 모드: AI 자동 컨텍스트 판단

**현재:** 수동 드롭다운으로 대화 모드 선택 (일반 대화 / 메모리 검색 등)
**변경:** 드롭다운 제거. AI가 사용자 메시지를 분석하여 자동으로 적절한 컨텍스트를 판단한다.

- 메모리 관련 질문 → 자동으로 벡터 검색 후 관련 메모리 참조
- 일반 대화 → 메모리 참조 없이 직접 응답
- 사용자는 모드를 신경쓰지 않고 자연스럽게 대화

### 2.3 Journal: 회고 중심 + 토픽 기반 글쓰기

**핵심 방향:** 매일 저녁 하루를 회고하는 습관 형성

- **권장 리듬:** 매일 저녁 (하지만 주간/수시 작성도 허용)
- **회고 중심:** "오늘 무엇을 읽었고, 무엇을 생각했는가"를 AI와 함께 정리
- **토픽 기반:** AI 다이제스트가 그날 수집된 메모리를 토픽별로 묶어 글감 제안
- **AI 보조:** 에디터 옆 AI 패널에서 질문 → 사고 확장 지원

### 2.4 GraphView: 잠재력 있는 차별화 포인트

GraphView는 데이터가 충분히 쌓이면 가치가 드러날 **잠재력 있는 차별화 포인트**다. 현재는 가능성 단계이며, P2에서 점진적으로 투자한다.

- 메모리 간 관계를 3D 그래프로 시각화
- 사용자가 자신의 지식 지도를 탐색하는 경험 제공
- P2에서 상호작용 강화: 노드 클릭 → 메모리 상세, 클러스터 탐색, 시간 흐름 애니메이션
- 확신이 아닌 가설 단계 — dogfooding으로 실제 가치를 검증한 후 투자 규모를 결정

### 2.5 능동적 넛지 시스템

사용자를 기다리지 않고 **프로액티브하게 참여를 유도**한다:

- **저녁 회고 알림:** "오늘 3개의 새로운 기억이 쌓였습니다. 저널을 작성해볼까요?"
- **주간 요약:** 이번 주 수집한 기억과 작성한 저널의 통계 + 인사이트
- **연결 발견:** "최근 저장한 기억이 2주 전 기억과 연결됩니다"
- 구현 시점: P2 (P2-10)

### 2.6 테마: 시스템 설정 자동 전환

`prefers-color-scheme` 미디어 쿼리로 OS 시스템 설정을 따른다.

- 이미 Light/Dark 이중 팔레트 구현 완료 (강점으로 확인)
- 수동 토글 추가하되, 기본값은 시스템 자동 전환
- 구현 시점: P1-12

### 2.7 수익화: 미정

현재는 수익화 모델을 확정하지 않는다. MVP 품질 향상과 dogfooding이 우선이다. 추후 사용자 데이터를 기반으로 결정한다.

**인프라 비용 원칙:** 현재 무료 티어(Supabase Free, Fly.io Free)로 운영한다. 비용 최소화가 원칙이며, 유료 전환이 필요한 시점에 별도 검토한다.

---

## Part 3. 현재 상태 진단

### 3.1 종합 점수표

| 평가 항목 | PM | UI/UX Master | UX Expert | 가중 평균 |
|-----------|-----|-------------|-----------|-----------|
| 기능 범위 (Feature Scope) | 8/10 | 7/10 | 7/10 | **7.4** |
| 기능 깊이 (Feature Depth) | 6/10 | 5/10 | 5/10 | **5.4** |
| UX 완성도 | 5/10 | 4/10 | 4/10 | **4.3** |
| 코드 품질 | 8/10 | 7/10 | - | **7.5** |
| PMF 준비도 | 4/10 | 3/10 | 3/10 | **3.3** |

> **PM 코멘트:** 기능 범위는 양호하다. Chat(SSE 스트리밍), Memory(CRUD + 벡터 검색), Journal(Tiptap WYSIWYG + AI 보조), Graph(3D 시각화) 4개 핵심 모듈이 모두 작동한다. 하지만 각 모듈의 깊이와 UX 완성도가 현저히 부족하여 PMF 준비 상태라 보기 어렵다. "넓지만 얕은" 전형적인 MVP 상태.

### 3.2 아키텍처 현황

```
프론트엔드: React + TypeScript + Vite
  - 5개 주요 뷰: ChatView, MemoryView, JournalView, GraphView, SettingsView
  - Sidebar 통합 네비게이션 (Chat/Memories/Journal/Graph/Settings)
  - 삭제된 뷰(Dashboard, Search, Timeline)는 리다이렉트 처리 완료

백엔드: FastAPI + Python
  - Supabase (PostgreSQL + pgvector) - 벡터 검색 및 데이터 저장
  - KuzuDB - 그래프 데이터
  - LangGraph 기반 에이전트 (Socrates: 대화, Librarian: 메모리 처리)
  - SSE 스트리밍 응답
```

### 3.3 강점 (3인 합의)

| 강점 | 상세 |
|------|------|
| 디자인 토큰 시스템 | NotebookLM 벤치마크 기반 CSS 변수 체계가 잘 잡혀있음. Light/Dark 이중 팔레트 구현 완료 |
| SSE 스트리밍 | ChatView의 실시간 응답 스트리밍이 안정적으로 동작 |
| JournalView 3-panel 설계 | 에디터 + AI 패널 + 메모리 사이드바 구조가 NotebookLM 수준의 작업 흐름 지원 |
| 아이콘 시스템 마이그레이션 | 이모지 -> Lucide React 전환이 대부분 완료됨 (Sidebar, ChatView, MemoryView 확인) |
| 라우팅 정리 | 삭제된 뷰에 대한 리다이렉트 처리가 깔끔함 |

---

## Part 4. 핵심 문제점 (10개 기존 + 4개 신규)

### 문제 1. API 에러 시 사용자 피드백 부재 [3인 만장일치 P0]

**현상:** 5개 주요 화면 중 3개(MemoryView의 전체/검색 탭, ChatView 히스토리 로드)에서 에러 발생 시 `console.error`만 호출하고 사용자에게는 아무 피드백이 없다.

**코드 근거:**
- `MemoryView.tsx:81` - `loadMemories` catch 블록: `console.error('메모리 목록 로드 실패:', error)`만 존재
- `MemoryView.tsx:108` - `addMemory` catch 블록: `console.error('메모리 추가 실패:', error)`만 존재
- `ChatView.tsx:112` - `loadHistory` catch 블록: `console.error('채팅 히스토리 로드 실패:', error)`만 존재
- `JournalView.tsx:101` - 다이제스트 로드 실패: `console.error`만 존재

**UX Expert 발언:** "SettingsView만 유일하게 토스트 패턴을 사용하고 있다. JournalView도 자체 토스트를 구현해놓았는데, 이 패턴이 다른 화면에는 전혀 전파되지 않았다."

**UI/UX Master 발언:** "사용자가 메모리를 추가하고 성공/실패를 모르는 상태는 가장 치명적인 UX 결함이다."

**PM 결론:** 글로벌 토스트 시스템을 구축하고 모든 비동기 작업에 일괄 적용한다. JournalView의 로컬 토스트 패턴을 참고하되 Context 기반 글로벌 시스템으로 승격시킨다.

---

### 문제 2. 메모리 카드 클릭 시 무반응 (상세보기 없음) [3인 만장일치 P0]

**현상:** MemoryView의 메모리 카드(`memory-card`)에 `onClick` 핸들러가 없다. 사용자가 카드를 클릭해도 아무 일도 일어나지 않는다. 상세보기, 수정, 삭제 기능이 모두 부재.

**코드 근거:** `MemoryView.tsx:226-238` - `<div className="memory-card glass-card">` 에 onClick 없음

**UI/UX Master 발언:** "카드가 시각적으로 hover 효과가 있어서 클릭 가능해 보이는데 실제로 아무 동작이 없다. 이것은 affordance 위반이다."

**UX Expert 발언:** "최소한 제목, 전체 요약, 태그, 원본 URL, 생성일, 관련 메모리 정도를 보여주는 상세 패널이 필요하다."

**PM 결론:** 메모리 상세 모달 또는 슬라이드 패널을 구현한다. 상세 화면에서 수정/삭제 기능도 함께 제공한다. 이것이 없으면 메모리 관리 자체가 불가능하므로 P0.

---

### 문제 3. 온보딩 플로우 부재 [PM + UX Expert P0, UI/UX Master P1]

**현상:** 첫 방문 사용자가 로그인 후 빈 Chat 화면만 보게 된다. 제품이 무엇인지, 어떻게 사용하는지에 대한 안내가 전혀 없다.

**UX Expert 발언:** "온보딩 위자드가 필수다. 최소 3단계: (1) 환영 + 제품 소개, (2) 첫 메모리 추가 유도, (3) 대화 시작 유도. 이것 없이 리텐션을 기대할 수 없다."

**UI/UX Master 발언:** "P1으로 봐도 된다. 빈 상태(empty state)에 가이드 텍스트를 잘 넣으면 온보딩 위자드 없이도 충분히 유도 가능하다."

**PM 판단:** P0으로 분류한다. 이유: Memoir는 "AI 지식 관리 도구"라는 개념 자체가 생소한 카테고리다. 사용자가 메모리를 먼저 추가해야 Chat이 의미있어지는 구조이므로, 첫 사용자 동선 설계 없이는 Day 1 이탈률이 극도로 높을 것이다. 단, 풀 온보딩 위자드가 아닌 "첫 방문 환영 배너 + 빈 상태 개선"으로 범위를 축소한다.

---

### 문제 4. `alert()` 사용 및 네이티브 confirm 잔존 [UX Expert P0, UI/UX Master P1]

**현상:** UX Expert가 코드베이스에서 `alert()` 호출 3곳을 보고했다.

**UI/UX Master 발언:** "현재 코드 검토 결과 ChatView와 MemoryView에서는 alert가 제거된 상태다. 잔존 위치를 정확히 확인해야 한다."

**PM 판단:** 잔존 alert()를 전수 조사하여 글로벌 토스트 또는 확인 다이얼로그로 교체한다. 문제 1의 글로벌 토스트 시스템 구축과 동시에 처리하므로 같은 티켓에서 해결.

---

### 문제 5. ChatView textarea auto-resize 미구현 [UI/UX Master P0]

**현상:** `ChatView.tsx:278-286`의 `<textarea rows={1}>` 이 고정 높이다. 여러 줄 입력 시 내부 스크롤이 생기며 사용자가 자신이 쓴 내용을 볼 수 없다.

**코드 근거:** `onChange={(e) => setInput(e.target.value)}` 에 auto-resize 로직 없음

**UI/UX Master 발언:** "모든 현대 채팅 UI의 기본이다. max-height까지 자동 확장 후 스크롤 전환하면 된다."

**PM 결론:** P0 동의. 구현 난이도 낮음(0.5d). `scrollHeight` 기반 auto-resize + `max-height: 200px` 제한.

---

### 문제 6. 사이드바 "새 대화" 버튼 누락 [UI/UX Master + UX Expert P0-P1]

**현상:** `Sidebar.tsx`에 "새 대화" 시작 버튼이 없다. DESIGN_SPEC.md에서 "New Chat" 필(pill) 버튼을 명시했으나 구현되지 않았다.

**코드 근거:** `Sidebar.tsx:79-93` - nav 섹션에 CTA 버튼 없음. 새 대화를 시작하려면 Chat 네비게이션 항목을 다시 클릭해야 하는데 이것도 새 세션을 생성하지 않음.

**UI/UX Master 발언:** "P0. Chat 네비게이션 클릭 시 `end` prop이 있어서 `/chat` 경로에서만 active 되지만, 이미 `/chat`에 있으면 아무 일도 안 일어난다."

**UX Expert 발언:** "P1으로 충분하다. 현재 구조에서 새 세션은 메시지를 보내면 자동 생성되니까."

**PM 판단:** P0으로 올린다. 사용자 입장에서 "새 대화를 어떻게 시작하지?"라는 질문에 답이 없는 상태다. DESIGN_SPEC에 이미 스펙이 있으므로 구현만 하면 된다. 사이드바 상단, nav 위에 `+ 새 대화` 필 버튼 추가. 클릭 시 `/chat`으로 `{ state: { newSession: true } }` 전달.

---

### 문제 7. 한/영 혼재 (언어 일관성) [UX Expert P0, UI/UX Master P1]

**현상:**
- Sidebar 메뉴: "Chat", "Memories", "Journal", "Graph", "Settings" (전부 영문)
- JournalView 헤더: "Today's Journal" (영문)
- MemoryView 헤더: "Memories" (영문)
- ChatView 헤더: "Socrates" (영문 제품명은 허용)
- EditorToolbar 모드: "WYSIWYG" (기술 용어 노출)
- AuthView OAuth 버튼: 영문 (단, 이것은 OAuth 관행상 허용)

**UX Expert 발언:** "한국어 타겟 서비스라면 네비게이션과 헤더를 한국어로 통일해야 한다. 'WYSIWYG'는 일반 사용자가 이해할 수 없는 기술 용어다."

**UI/UX Master 발언:** "영문이 더 깔끔하다. 국제화(i18n) 가능성을 열어두려면 영문 유지가 낫다."

**PM 판단:** P1으로 분류한다. 현재 타겟이 한국어 사용자이므로 네비게이션 라벨은 한국어로 변경한다("대화", "기억", "저널", "그래프", "설정"). 단, 제품명("Memoir", "Socrates")과 OAuth 버튼은 영문 유지. "WYSIWYG" -> "편집"으로 변경. 향후 i18n 도입 시 라벨을 키-값으로 분리하는 것은 P2.

---

### 문제 8. 모달 접근성 (focus trap, Escape, ARIA) [UI/UX Master P1, UX Expert P2]

**현상:** MemoryView 추가 모달과 JournalView 세션 선택 모달에 focus trap, Escape 키 닫기, ARIA 속성이 모두 없다.

**코드 근거:**
- `MemoryView.tsx:464` - overlay `onClick` 닫기는 있으나 `onKeyDown` Escape 처리 없음
- 모달 열림 시 배경 스크롤 방지 없음
- `role="dialog"`, `aria-modal="true"`, `aria-labelledby` 없음

**UI/UX Master 발언:** "Escape 닫기와 focus trap은 반드시 P1이다. 키보드 사용자가 모달 뒤의 요소와 상호작용할 수 있는 상태는 심각한 접근성 위반이다."

**UX Expert 발언:** "P2로 충분하다. MVP 단계에서 접근성 완성도보다 핵심 기능 깊이가 우선이다."

**PM 판단:** Escape 키 닫기는 P0 (구현 비용 < 5분), focus trap + ARIA는 P1로 분류한다. focus trap은 커스텀 훅 하나로 모든 모달에 일괄 적용 가능하다.

---

### 문제 9. 반응형 대응 미비 [PM P2, UI/UX Master P1, UX Expert P1]

**현상:** 사이드바가 고정 240px로 태블릿/모바일에서 콘텐츠 영역을 과도하게 차지한다. DESIGN_SPEC.md에 반응형 브레이크포인트가 정의되어 있으나 미구현.

**UI/UX Master 발언:** "P1. 태블릿 사용자가 실제로 있을 수 있다. 아이콘 전용 모드(56px)와 모바일 햄버거 메뉴는 기본이다."

**UX Expert 발언:** "P1. JournalView의 3-panel 구조가 1024px 이하에서 완전히 깨진다. MemorySidebar가 에디터를 압축해서 사용 불가 상태."

**PM 판단:** P1로 분류하되, 범위를 제한한다. 전체 반응형이 아니라 (1) 사이드바 모바일 대응, (2) JournalView 레이아웃 스택 전환 두 가지만 먼저 처리한다. 나머지 그리드 반응형은 P2.

---

### 문제 10. `glass-card` 클래스 잔존 [UI/UX Master P1, UX Expert P1]

**현상:** DESIGN_SPEC.md에서 glassmorphism 제거를 명시했으나, MemoryView에 `glass-card` 클래스가 여전히 5곳 이상 남아있다.

**코드 근거:**
- `MemoryView.tsx:226` - `memory-card glass-card`
- `MemoryView.tsx:286` - `timeline-item glass-card`
- `MemoryView.tsx:323` - `search-box glass-card`
- `MemoryView.tsx:392` - `result-card glass-card`
- `MemoryView.tsx:465` - `modal-content glass-card`

**PM 결론:** P1. DESIGN_SPEC 리디자인의 미완성 부분. 모든 `glass-card` 참조를 `card` 또는 적절한 대체 클래스로 교체하고, CSS에서 `glass-card` 규칙을 제거한다.

---

### 문제 11. [신규] 벡터 검색 user_id 필터 누락 [보안 취약점 P0]

**현상:** 벡터 검색(pgvector) 쿼리에 `user_id` 필터가 누락되어 있을 가능성이 있다. 이 경우 사용자 A의 검색 쿼리에 사용자 B의 메모리가 결과로 반환될 수 있다.

**심각도:** **크로스 유저 데이터 노출** — 개인 지식 관리 도구에서 가장 치명적인 보안 결함.

**PM 판단:** P0 최우선. 모든 벡터 검색 쿼리(`searchService`, `chat` 에이전트의 RAG 검색, `relatedMemories` 등)에서 `user_id` 필터가 적용되고 있는지 전수 조사 후 즉시 수정한다.

---

### 문제 12. [신규] Chat 모드 드롭다운 제거 + AI 자동 판단 [P1]

**현상:** 현재 ChatView에 대화 모드를 수동으로 선택하는 드롭다운이 있다. 이는 사용자에게 불필요한 인지 부하를 주며, "어떤 모드를 선택해야 하지?"라는 질문을 유발한다.

**전략 결정 (Q&A 2.2):** 드롭다운을 제거하고 AI가 사용자 메시지를 분석하여 자동으로 컨텍스트를 판단하도록 변경한다.

**PM 판단:** P1. 백엔드 Socrates 에이전트의 라우팅 로직 수정이 필요하므로 프론트엔드 단독 작업이 아님. LangGraph 에이전트의 의도 분류 노드 추가/수정 포함.

---

### 문제 13. [신규] 능동적 넛지 시스템 부재 [P2]

**현상:** 현재 Memoir는 완전히 수동적이다. 사용자가 직접 접속하여 작업을 시작하지 않으면 아무 일도 일어나지 않는다.

**전략 결정 (Q&A 2.5):** 저녁 회고 알림, 주간 요약, 연결 발견 알림 등 프로액티브 넛지 시스템을 도입한다.

**PM 판단:** P2. 알림 인프라(이메일, 브라우저 푸시, 카카오톡 등) 구축이 선행되어야 하므로 공수가 큼. 하지만 리텐션에 핵심적인 기능이므로 P2 초기에 착수.

---

### 문제 14. [신규] 저널 AI 보조 기능 부족 [P1]

**현상:** 파운더가 매일 저널을 쓰고 싶지만 빈 에디터 앞에서 뭘 쓸지 모르겠다. AI 다이제스트가 글감 제안으로는 부족하고, 에디터 UX도 불편하다. "쓰고 싶은데 불편해서 못 쓴다"가 핵심 장벽.

**필요 기능:**
- 회고 질문 자동 생성 ("오늘 가장 인상적인 읽을거리는?", "이번 주 가장 많이 고민한 주제는?")
- 메모리 기반 초안 작성 및 토픽 추출 강화
- 구조화된 회고 템플릿 제공 ("오늘의 TIL", "이번 주 회고", "프로젝트 회고")

**PM 판단:** P1. 파운더가 매일 사용하려면 저널 진입 장벽을 낮추는 것이 핵심. AI가 "오늘 뭘 쓸까?"에 대한 답을 제시해야 한다. P1 초반(P1-1 ~ P1-3 이후)에 배치하여 dogfooding 가치를 빠르게 높인다.

---

## Part 5. P0/P1/P2 로드맵

### P0: 즉시 수정 (이번 스프린트, 1주 내)

| # | 작업 항목 | 예상 공수 | 담당 | 선행 조건 | 상세 |
|---|----------|----------|------|----------|------|
| P0-1 | 글로벌 토스트/알림 시스템 구축 | 1d | 프론트엔드 | 없음 | `ToastContext` + `ToastProvider` 생성. `useToast()` 훅으로 `success/error/info` 3종 알림 호출. 하단 중앙 고정, 3초 자동 소멸. JournalView 로컬 토스트를 글로벌로 마이그레이션. |
| P0-2 | 모든 catch 블록에 사용자 피드백 추가 | 0.5d | 프론트엔드 | P0-1 | ChatView(loadHistory, sendMessage), MemoryView(loadMemories, addMemory, search), JournalView(digest, relatedMemories) 총 8개 catch 블록에 `toast.error()` 호출 추가. `console.error`는 유지(디버깅용). |
| P0-3 | 메모리 추가 성공 피드백 | 0.5d | 프론트엔드 | P0-1 | `addMemory` 성공 시 `toast.success('메모리가 추가되었습니다')` 호출. 현재는 모달만 닫히고 피드백 없음. |
| P0-4 | 메모리 상세 모달 | 2d | 프론트엔드 + 백엔드 | 없음 | 카드 클릭 -> 상세 모달: 제목, 전체 요약, 태그, 원본 URL(WEB), 생성일, 관련 메모리(기존 `searchService.getRelatedMemories` 활용). 모달 내 삭제 버튼(confirm 다이얼로그 후 API 호출). 백엔드: `DELETE /api/memories/{id}` 엔드포인트 확인/추가. |
| P0-5 | ChatView textarea auto-resize | 0.5d | 프론트엔드 | 없음 | `onChange` 핸들러에서 `textarea.style.height = 'auto'; textarea.style.height = Math.min(textarea.scrollHeight, 200) + 'px'` 패턴 적용. CSS에 `resize: none; overflow-y: auto; max-height: 200px;` 추가. |
| P0-6 | 사이드바 "새 대화" 버튼 | 0.5d | 프론트엔드 | 없음 | `Sidebar.tsx` nav 상단에 `<button className="btn-new-chat">+ 새 대화</button>` 추가. 클릭 시 `navigate('/chat', { state: { newSession: true } })`. 디자인: 필 버튼(border-radius: 9999px), width: 100%, height: 36px. |
| P0-7 | 모달 Escape 키 닫기 | 0.5d | 프론트엔드 | 없음 | 모든 모달 컴포넌트에 `useEffect` 로 `keydown` Escape 이벤트 리스너 추가. MemoryView 추가 모달, JournalView 세션 선택 모달 대상. |
| P0-8 | 첫 방문 환영 배너 | 1d | 프론트엔드 | P0-1 | 메모리가 0개인 사용자에게 ChatView에 환영 카드 표시: "Memoir에 오신 것을 환영합니다! 먼저 웹 페이지나 메모를 저장해보세요." + [메모리 추가하기] 버튼 (-> `/memories`로 이동). `localStorage`에 `onboarding_dismissed` 플래그 저장. |
| P0-9 | 벡터 검색 user_id 필터 보안 수정 | 0.5d | 백엔드 | 없음 | 모든 벡터 검색 쿼리에 `user_id` 필터 적용 여부 전수 조사. 누락된 곳에 즉시 필터 추가. `searchService`, RAG 검색, `relatedMemories` 대상. |

**P0 합계: 약 7일 (프론트엔드 6d, 백엔드 1d)**

---

### P1: 품질 향상 (2주차)

| # | 작업 항목 | 예상 공수 | 담당 | 선행 조건 | 상세 |
|---|----------|----------|------|----------|------|
| P1-1 | `glass-card` 전면 교체 | 1d | 프론트엔드 | 없음 | MemoryView 내 5곳 + CSS 파일에서 `.glass-card` 규칙 제거. DESIGN_SPEC의 `.card` 패턴으로 대체(flat border, no backdrop-filter). |
| P1-2 | 네비게이션/UI 한국어화 | 0.5d | 프론트엔드 | 없음 | Sidebar: Chat->대화, Memories->기억, Journal->저널, Graph->그래프, Settings->설정. JournalView: "Today's Journal" -> "오늘의 저널". EditorToolbar: "WYSIWYG" -> "편집", "markdown" -> "마크다운", "viewer" -> "미리보기". |
| P1-3 | 모달 focus trap + ARIA | 1d | 프론트엔드 | P0-7 | `useFocusTrap` 커스텀 훅 생성. 모든 모달에 `role="dialog"`, `aria-modal="true"`, `aria-labelledby` 추가. 모달 열림 시 `document.body.style.overflow = 'hidden'`. |
| P1-4 | 채팅 세션 제목 자동 생성 | 1.5d | 백엔드 + 프론트엔드 | 없음 | 첫 메시지 전송 후 LLM으로 세션 제목 요약 생성(비동기). 백엔드: `PATCH /api/chat/sessions/{id}` 제목 업데이트. 프론트: 사이드바 세션 목록에 제목 반영. 현재 세션 제목이 "New Chat" 고정인 것으로 추정. |
| P1-5 | 저널 자동 저장 (localStorage) | 1d | 프론트엔드 | 없음 | 에디터 내용 변경 시 디바운스(2초)로 `localStorage.setItem('journal_draft', content)` 저장. 페이지 진입 시 로컬 초안이 있으면 복원 여부 확인 토스트 표시. 서버 저장 성공 시 로컬 초안 삭제. |
| P1-6 | 반응형 사이드바 | 1.5d | 프론트엔드 | 없음 | 768-1023px: 아이콘 전용 56px, hover 시 확장. <768px: 숨김 + 햄버거 메뉴. 사이드바 상태를 `useState` + `matchMedia` 연동. |
| P1-7 | JournalView 반응형 스택 전환 | 1d | 프론트엔드 | P1-6 | 1024px 이하에서 MemorySidebar를 에디터 하단으로 스택. 접는(collapsible) 패널로 전환. |
| P1-8 | 채팅 영역 ARIA live region | 0.5d | 프론트엔드 | 없음 | 메시지 영역에 `aria-live="polite"` 추가. 스크린 리더가 새 메시지 도착을 인지할 수 있도록. |
| P1-9 | 하드코딩 색상/그림자 CSS 변수 교체 | 1d | 프론트엔드 | 없음 | 커밋 53e790d에서 24건 교체했으나 잔존분 확인. 특히 MemoryView.css, GraphView의 `NODE_COLORS` 인라인 색상(현재는 의도적이므로 제외 가능), 기타 `.css` 파일 스캔. |
| P1-10 | 사용자 메시지 버블색 조정 | 0.5d | 프론트엔드 | 없음 | 현재 사용자/어시스턴트 메시지 시각적 구분이 아이콘에만 의존. 사용자 메시지에 `var(--accent-bg)` 배경 또는 우측 정렬 추가(DESIGN_SPEC은 좌측 정렬 통일을 권장하나, 배경색 차이로 구분). |
| P1-11 | Chat 모드 드롭다운 제거 + AI 자동 판단 | 2d | 풀스택 | 없음 | 프론트: 모드 선택 드롭다운 UI 제거. 백엔드: Socrates 에이전트에 의도 분류 로직 추가 — 사용자 메시지 분석 후 메모리 검색 필요 여부를 AI가 자동 판단. LangGraph 라우팅 노드 수정. |
| P1-12 | 시스템 테마 자동 전환 | 0.5d | 프론트엔드 | 없음 | `prefers-color-scheme` 미디어 쿼리로 OS 설정 자동 감지. 기존 Light/Dark 팔레트 활용. SettingsView에 수동 토글 옵션 유지(시스템/라이트/다크 3택). |
| P1-13 | 저널 AI 강화 | 2d | 풀스택 | P1-1~3 이후 | 회고 질문 자동 생성("오늘 가장 인상적인 읽을거리는?"), 메모리 기반 초안/토픽 추출 강화, 회고 템플릿 제공("오늘의 TIL", "이번 주 회고"). P1 초반 배치로 dogfooding 가치를 빠르게 높인다. |

**P1 합계: 약 14일 (프론트엔드 10.5d, 백엔드 3.5d)**

---

### P2: 제품 성숙 (1개월)

| # | 작업 항목 | 예상 공수 | 담당 | 선행 조건 | 상세 |
|---|----------|----------|------|----------|------|
| P2-1 | 저널 히스토리 (과거 저널 조회) | 3d | 풀스택 | P1-5 | 백엔드: `GET /api/journal/history` (날짜별 목록), `GET /api/journal/{date}` (특정 날짜 저널). 프론트: JournalView에 날짜 선택 캘린더 또는 리스트 추가. |
| P2-2 | 리텐션 대시보드 (스트릭, 추세) | 3d | 풀스택 | P2-1 | 저널 작성 스트릭, 주간 메모리 추세, 주제 변화 시각화. 감정적 보상 요소: 연속 기록 축하 메시지. |
| P2-3 | 글로벌 검색 (Cmd+K) | 2d | 프론트엔드 | 없음 | 키보드 단축키로 팔레트 열림. 메모리, 채팅 세션, 저널을 통합 검색. 기존 벡터 검색 API 재활용. |
| P2-4 | GraphView 전면 한국어화 | 1d | 프론트엔드 | 없음 | 노드 타입 라벨, 범례, 정보 패널의 영문 텍스트 한국어 전환. |
| P2-5 | 모바일 반응형 완성 | 3d | 프론트엔드 | P1-6, P1-7 | 모든 뷰의 모바일 레이아웃 최적화. 터치 인터랙션 고려. |
| P2-6 | 키보드 접근성 강화 | 2d | 프론트엔드 | P1-3 | 전체 탭 순서 구현, 커스텀 컴포넌트 키보드 네비게이션. GraphView 키보드 탐색. |
| P2-7 | 다이제스트 봇 스케줄러 | 2d | 백엔드 | 없음 | 매일 저녁 자동 다이제스트 생성 + 알림. Celery/APScheduler 기반. |
| P2-8 | KuzuDB 영속성 확인 | 1d | 인프라 | 없음 | Fly.io 볼륨 마운트 설정 확인. 컨테이너 재시작 시 그래프 데이터 유실 여부 테스트. |
| P2-9 | Chrome Extension 개선 | 3d | 프론트엔드 | 없음 | 현재 웹 페이지를 원클릭으로 메모리에 추가하는 기능 강화. |
| P2-10 | 능동적 넛지 시스템 | 5d | 풀스택 | P2-7 | 저녁 회고 알림, 주간 요약, 연결 발견 알림. 알림 채널: 브라우저 푸시 + 이메일. 사용자별 알림 설정 UI. 스케줄러(P2-7)와 연동. |
| P2-11 | GraphView 상호작용 강화 | 3d | 프론트엔드 | 없음 | 노드 클릭 → 메모리 상세 연결. 클러스터 탐색 UI. 시간 흐름 필터. 검색 결과 하이라이트. dogfooding으로 가치 검증 후 투자 규모 결정. |

**P2 합계: 약 28일**

---

## Part 6. 의견 대립 및 합의

### 대립 1: 접근성(ARIA/focus trap)의 우선순위

| 입장 | 근거 |
|------|------|
| **UI/UX Master: P1 필수** | "WCAG 2.1 AA 기준은 법적 요구사항이 될 수 있다. 모달 focus trap 없이 배포하면 접근성 감사에서 실패한다." |
| **UX Expert: P2로 미룸** | "MVP 단계에서는 핵심 기능 완성도가 우선이다. 접근성은 사용자 기반이 확보된 후에 투자해도 된다." |

**PM 최종 결정:** 이분법으로 나누지 않는다.
- Escape 키 닫기: **P0** (5분이면 구현 가능, 기본 UX)
- focus trap + ARIA: **P1** (커스텀 훅 1개로 모든 모달 적용 가능, 1일 공수)
- 전체 키보드 네비게이션: **P2** (GraphView 등 복잡한 컴포넌트 포함)

**합의 근거:** 접근성을 '전부 또는 전무'로 볼 필요 없다. 비용 대비 효과가 높은 항목부터 점진적으로 적용한다.

---

### 대립 2: 반응형 대응 시점

| 입장 | 근거 |
|------|------|
| **PM: P2** | "데스크톱 웹 우선. 모바일은 향후 PWA 또는 네이티브 앱으로 별도 대응할 수 있다." |
| **UI/UX Master + UX Expert: P1** | "JournalView가 태블릿에서 깨지는 건 버그다. 최소한 사이드바와 JournalView만이라도 대응해야 한다." |

**PM 최종 결정:** P1로 격상하되 범위를 제한한다.
- **P1 범위:** 사이드바 반응형(아이콘 전용 + 햄버거) + JournalView 레이아웃 스택 전환
- **P2 범위:** 나머지 뷰의 그리드 반응형 + 모바일 터치 최적화

**합의 근거:** "깨지는 레이아웃은 기능 부재가 아니라 버그"라는 UI/UX Master의 의견에 동의. 사용할 수 없는 상태를 방치하는 것은 P2가 아니다.

---

### 대립 3: 네비게이션 언어 (한국어 vs 영문)

| 입장 | 근거 |
|------|------|
| **UX Expert: 한국어 필수** | "타겟 사용자가 한국어 사용자다. 'Memories'보다 '기억'이 직관적이다." |
| **UI/UX Master: 영문 유지** | "i18n 가능성을 열어두고, 디자인적으로 영문이 더 깔끔하다." |

**PM 최종 결정:** 한국어로 변경한다. (P1)
- 사이드바 라벨: 대화, 기억, 저널, 그래프, 설정
- 제품명(Memoir, Socrates)과 OAuth 버튼은 영문 유지
- i18n 키-값 분리는 P2에서 별도 처리

**합의 근거:** 현재 제품의 타겟 시장이 한국이고, 한국어 마이크로카피가 이미 전반적으로 사용되고 있다(저널 날짜, 에러 메시지, 빈 상태 텍스트 등). 네비게이션만 영문인 것은 일관성 결여다.

---

### 대립 4: 온보딩 구현 범위

| 입장 | 근거 |
|------|------|
| **UX Expert: 풀 온보딩 위자드** | "3단계 위자드: 환영 -> 첫 메모리 추가 -> 첫 대화. 스텝 인디케이터, 건너뛰기 버튼 포함." |
| **UI/UX Master: 빈 상태 개선으로 충분** | "각 화면의 empty state에 적절한 가이드 텍스트와 CTA를 넣으면 별도 위자드 없이도 유도 가능." |

**PM 최종 결정:** 두 가지를 조합한다.
- **P0:** 첫 방문 환영 배너 (ChatView) + 개선된 빈 상태 CTA (MemoryView)
- **P2:** 풀 온보딩 위자드 (사용자 데이터 기반으로 최적 동선 검증 후)

**합의 근거:** 풀 온보딩 위자드는 2-3일 공수가 드는데, 현재 사용자 데이터 없이 "최적 온보딩 경로"를 설계하는 것은 추측에 기반한다. 먼저 최소한의 유도 장치를 넣고, 실제 이탈 데이터를 보고 위자드를 설계한다.

---

## Part 7. 스프린트 계획

### 스프린트 1 (2026-02-12 ~ 2026-02-19)

**목표:** P0 전체 완료. 사용자가 "사용할 수 있는" 상태로 만들기.

| 순서 | 작업 | 담당 | 일정 | 체크리스트 |
|------|------|------|------|-----------|
| 1 | 벡터 검색 user_id 보안 수정 (P0-9) | BE | 2/12 | [ ] 전수 조사: searchService, RAG, relatedMemories [ ] 누락 필터 추가 [ ] 테스트 검증 |
| 2 | `ToastContext` + `ToastProvider` + `useToast` 구현 | FE | 2/12-2/13 | [ ] Context 생성 [ ] Provider를 App 최상단에 래핑 [ ] success/error/info 3종 스타일링 [ ] 3초 auto-dismiss + 수동 닫기 |
| 3 | 전체 catch 블록 토스트 연결 + alert() 전수 조사 및 교체 | FE | 2/13 | [ ] ChatView 2곳 [ ] MemoryView 3곳 [ ] JournalView 2곳 [ ] alert() grep 후 전수 교체 |
| 4 | ChatView textarea auto-resize | FE | 2/13 | [ ] scrollHeight 기반 resize [ ] max-height: 200px [ ] rows 제거, min-height로 전환 |
| 5 | 사이드바 "새 대화" 버튼 | FE | 2/14 | [ ] 필 버튼 UI [ ] navigate with newSession state [ ] 기존 ChatView useEffect와 연동 확인 |
| 6 | 모달 Escape 키 닫기 | FE | 2/14 | [ ] MemoryView 추가 모달 [ ] JournalView 세션 선택 모달 [ ] useEffect keydown 리스너 |
| 7 | 메모리 상세 모달 (프론트) | FE | 2/14-2/17 | [ ] MemoryDetailModal 컴포넌트 [ ] 카드 onClick 연결 [ ] 제목, 요약, 태그, URL, 생성일 표시 [ ] 삭제 버튼 + confirm |
| 8 | 메모리 삭제 API 확인/추가 | BE | 2/14 | [ ] DELETE /api/memories/{id} 엔드포인트 [ ] 벡터 임베딩 정리 [ ] 그래프 노드 정리 |
| 9 | 첫 방문 환영 배너 | FE | 2/17-2/18 | [ ] 메모리 0개 조건 체크 [ ] 환영 카드 UI [ ] "메모리 추가하기" CTA [ ] localStorage dismissed 플래그 |
| 10 | 통합 QA | QA | 2/18-2/19 | [ ] 전체 토스트 동작 확인 [ ] 메모리 CRUD 플로우 [ ] 새 사용자 시나리오 [ ] 키보드 Escape 확인 [ ] 벡터 검색 보안 검증 |

### 스프린트 2 (2026-02-19 ~ 2026-03-05)

**목표:** P1 항목 처리. 디자인 시스템 정합성 + 접근성 기초 + 반응형 기초 + 전략적 기능 개선.

**핵심 작업:**
1. `glass-card` 전면 교체 (1d)
2. 네비게이션/UI 한국어화 (0.5d)
3. 모달 focus trap + ARIA (1d)
4. **저널 AI 강화 (2d)** — 신규: 회고 질문 생성, 초안/토픽 추출, 회고 템플릿
5. 채팅 세션 제목 자동 생성 (1.5d)
6. 저널 자동 저장 localStorage (1d)
7. 반응형 사이드바 (1.5d)
8. JournalView 반응형 스택 전환 (1d)
9. 하드코딩 CSS 변수 잔존분 교체 (1d)
10. Chat 모드 드롭다운 제거 + AI 자동 판단 (2d)
11. 시스템 테마 자동 전환 (0.5d)

---

### 기술적 선행 조건 정리

```
P0-1 (글로벌 토스트) ──> P0-2 (catch 블록 연결)
                      ──> P0-3 (메모리 추가 피드백)
                      ──> P0-8 (환영 배너, 토스트 활용)

P0-7 (Escape 닫기) ──> P1-3 (focus trap + ARIA)

P1-1~3 (디자인 정합성) ──> P1-13 (저널 AI 강화)

P1-6 (반응형 사이드바) ──> P1-7 (JournalView 반응형)
                        ──> P2-5 (모바일 반응형 완성)

P1-5 (저널 자동 저장) ──> P2-1 (저널 히스토리)
                       ──> P2-2 (리텐션 대시보드)

P2-7 (다이제스트 스케줄러) ──> P2-10 (능동적 넛지)
```

---

## Part 8. 향후 전략 로드맵

### Phase 1: MVP 품질 향상 → "파운더가 매일 사용할 수 있는" 상태 (현재 ~ P0 완료)

**목표:** 파운더가 매일 사용할 수 있는 기본 사용성 확보. 핵심 플로우(수집 → 대화 → 회고)를 막힘 없이 수행할 수 있는 상태.

**핵심 작업:**
- P0 전체 (글로벌 피드백, 메모리 상세보기, 온보딩, 보안 수정)

**핵심 메트릭 (dogfooding 기준):**
- 파운더가 주 5일 이상 접속하여 실사용 가능
- 핵심 플로우(수집 → 대화 → 회고)에서 막힘 0건
- 크리티컬 버그 0건 (특히 보안 이슈)

---

### Phase 2: 핵심 경험 심화 → "주변 지인이 사용할 수 있는" 상태 (P1 + P2 초기)

**목표:** 주변 지인 3명이 일주일간 사용 가능한 상태. 차별화된 경험 완성으로 기존 도구(Notion, Obsidian)와 다른 가치를 체감할 수 있는 수준.

**핵심 작업:**
- P1 전체 (디자인 정합성, 접근성, 반응형, AI 자동 판단, 저널 AI 강화)
- P2 초기: 저널 히스토리, GraphView 강화, 글로벌 검색

**핵심 메트릭 (dogfooding 확장 기준):**
- 주변 지인 3명에게 배포하여 일주일간 사용 가능 (피드백 수집 가능 상태)
- 파운더 외 사용자가 온보딩 ~ 첫 저널 작성까지 도움 없이 완료 가능
- 피드백에서 "사용 불가" 수준의 블로커 0건

---

### Phase 3: 성장 엔진 → 외부 유저 확보 시점 (P2 후기)

**목표:** 외부 사용자를 받을 준비가 된 상태. 자체 성장 루프 구축의 기초.

**핵심 작업:**
- 능동적 넛지 시스템 (저녁 회고 알림, 주간 요약)
- 리텐션 대시보드 (스트릭, 성취감)
- Chrome Extension 강화 (수집 허들 최소화)
- 풀 온보딩 위자드 (데이터 기반 최적화)

**핵심 메트릭 (외부 유저 확보 시점에 정량 메트릭 도입):**
- Phase 2까지의 dogfooding 데이터를 기반으로 정량 메트릭 설정
- 도입 예정 메트릭: Day 1 이탈률, WAU 재방문율, MAU 성장률, Day 7 리텐션
- 외부 유저 모집 전까지는 파운더 + 지인 피드백 기반 정성적 평가

---

### PM 최종 코멘트

> 현재 Memoir는 "기술적으로는 동작하지만 파운더조차 매일 쓰기 불편한" 상태다. 가장 시급한 것은 **피드백 루프 완성**(토스트, 상세보기, 성공/실패 알림)과 **보안 기본기**(벡터 검색 user_id 필터), 그리고 **저널 진입 장벽 해소**(AI 질문 생성, 템플릿, 초안 보조)이다. 파운더가 자신의 도구를 매일 쓸 수 없다면 외부 사용자에게 내놓을 준비가 된 것이 아니다.
>
> 전략적으로는 "통합 기억 관리"라는 핵심 가치와 "멀티채널 수집 + AI 자동 정리 + 소크라테스 대화"라는 차별화 3축이 확정되었다. 이 방향에 맞춰 Phase 1에서 파운더가 매일 쓸 수 있는 기초를 다지고, Phase 2에서 주변 지인에게 확장하고, Phase 3에서 외부 사용자를 받을 준비를 한다.
>
> P0의 7일 투자로 "파운더가 매일 사용할 수 있는" 상태가 되고, P1의 14일 투자로 "지인에게 보여줄 수 있는" 상태에 근접할 수 있다. 현재는 상용 제품이 아닌 dogfooding 단계임을 인정하고, 현실적인 목표를 하나씩 달성해나가는 것이 핵심이다.
